# 实验5 神经网络实现（MLP vs CNN）

- **做了啥**：手写 MLP 和用 PyTorch 写 CNN 识别数字。
- **遇到的问题**：手写 MLP 的时候，矩阵乘法维度一直对不上，报错报到崩溃。反向传播公式推导不出来，最后好像是抄的公式。CNN 倒是好写，但不知道卷积核大小该设多少。
- **解决方案**：MLP 凑合跑通了，准确率不高。CNN 直接用了课件里的结构，没怎么改参数，准确率居然还行。
- **启发**：还是用框架香，手写反向传播太痛苦了；CNN 对图像确实比 MLP 厉害。
